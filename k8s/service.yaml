apiVersion: v1
kind: Service
metadata:
  name: nodejs-app
  namespace: simple-app
  labels:
    app: nodejs-app
spec:
  ports:
  - port: 80
    targetPort: 3000
    protocol: TCP
  selector:
    app: nodejs-app
  type: ClusterIP
